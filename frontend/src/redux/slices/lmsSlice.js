// Placeholder Redux slice for LMS
import { createSlice } from '@reduxjs/toolkit';
const lmsSlice = createSlice({
  name: 'lms',
  initialState: {},
  reducers: {}
});
export default lmsSlice.reducer;
